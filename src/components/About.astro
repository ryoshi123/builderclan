<section class="about section-jump-target" id="about" data-reveal="fade-up">
	<div class="content-container about__content">
		<h2 class="section-heading">About</h2>
		<p class="section-copy">
			We are a community driven by curiosity and collaboration. From hackathons
			to workshops, BuilderClan@AISAT thrives on sharing knowledge, sparking
			innovation, and building things that matter.
		</p>
	</div>
</section>

<style>
	.about {
		position: relative;
		width: var(--section-full-width);
		margin: var(--space-page-reset);
		padding: var(--space-about-section-block) var(--space-section-gutter);
		color: var(--color-text-on-dark);
		overflow: hidden;
	}

	.about::before {
		position: absolute;
		inset: var(--section-ambient-inset);
		content: '';
		pointer-events: none;
		background: var(--background-section-ambient);
		filter: blur(var(--section-ambient-blur));
	}

	.about__content {
		position: relative;
		text-align: center;
	}
</style>

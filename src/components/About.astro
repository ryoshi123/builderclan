<section class="about section-jump-target" id="about" data-reveal="fade-up">
	<div class="about__ambient" aria-hidden="true"></div>
	<div class="content-container about__content">
		<h2 class="section-heading">About</h2>
		<p class="section-copy">
			We are a community driven by curiosity and collaboration. From hackathons
			to workshops, BuilderClan@AISAT thrives on sharing knowledge, sparking
			innovation, and building things that matter.
		</p>
	</div>
</section>

<style>
	.about {
		position: relative;
		width: var(--section-full-width);
		padding: var(--space-parent-section-block) var(--space-page-reset);
		overflow: hidden;
		color: var(--color-text-on-dark);
		background: var(--color-page-background);
	}

	.about__ambient {
		position: absolute;
		top: var(--game-frame-position);
		left: var(--game-frame-position);
		width: min(var(--section-full-width), var(--parent-section-glow-width));
		height: var(--parent-section-glow-height);
		pointer-events: none;
		opacity: var(--parent-ambient-opacity);
		background: var(--background-parent-ambient);
		filter: blur(var(--parent-menu-blur));
		transform: translate(var(--parent-center-x), var(--parent-center-x));
	}

	.about__content {
		position: relative;
		z-index: var(--parent-content-z);
		max-width: var(--parent-copy-max-width);
		text-align: center;
	}

	.about .section-heading {
		color: var(--color-brand-primary);
	}

	.about .section-copy {
		margin-inline: var(--space-centered);
	}
</style>

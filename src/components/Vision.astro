<section class="vision" data-reveal="slide-up">
	<div class="content-container">
		<article class="vision__card">
			<div class="vision__accent" aria-hidden="true"></div>
			<div class="vision__content">
				<h3>Our Vision</h3>
				<p>
					We envision a world where creativity meets action, where communities thrive
					through collaboration, and where every member of BuilderClan AISAT has the
					tools to turn their ideas into impact.
				</p>
			</div>
		</article>
	</div>
</section>

<style>
	.vision {
		padding: var(--space-page-reset) var(--space-vision-inline);
		background: var(--color-page-background);
		text-align: center;
	}

	.vision > .content-container {
		max-width: var(--vision-copy-max-width);
		padding-inline: var(--space-page-reset);
	}

	.vision__content {
		max-width: var(--vision-copy-max-width);
		margin-inline: var(--space-centered);
	}

	.vision h3 {
		margin: var(--space-vision-heading-top) var(--space-page-reset)
			var(--space-section-gutter);
		color: var(--color-brand-primary);
		font-size: var(--font-size-vision-heading);
		font-weight: var(--font-weight-section-heading);
		line-height: var(--line-height-feature-copy);
	}

	.vision p {
		margin: var(--space-page-reset);
		color: var(--color-inverted-section-text);
		font-size: var(--font-size-section-body);
		font-weight: var(--font-weight-paragraph);
		line-height: var(--line-height-feature-copy);
	}
</style>

---
import { navigationLinks } from '../data/navigation';

const logoSource = `${import.meta.env.BASE_URL}assets/images/builderclan/LogoShort.svg`;
---

<header class="site-header">
	<div class="site-header__inner">
		<div class="site-header__brand">
			<img
				src={logoSource}
				alt="BuilderClan Logo"
				class="site-header__mark"
			/>
			<a href="#hero" class="site-header__brand-link">
				<span class="site-header__brand-first">Builder</span><span
					class="site-header__brand-second">Clan</span
				>
			</a>
		</div>

		<nav class="site-header__links" aria-label="Primary navigation">
			{
				navigationLinks.map(({ label, href, external }) => (
					<a
						href={href}
						target={external ? '_blank' : undefined}
						rel={external ? 'noopener noreferrer' : undefined}
					>
						{label}
					</a>
				))
			}
		</nav>

		<button
			type="button"
			class="site-header__toggle"
			data-navigation-toggle
			data-open-label="Close navigation menu"
			aria-label="Open navigation menu"
			aria-controls="mobile-navigation"
			aria-expanded="false"
		>
			☰
		</button>
	</div>

	<nav
		class="site-header__overlay"
		id="mobile-navigation"
		data-navigation-overlay
		aria-label="Mobile navigation"
		aria-hidden="true"
		inert
		data-open="false"
	>
		{
			navigationLinks.map(({ label, href, external }) => (
				<a
					href={href}
					target={external ? '_blank' : undefined}
					rel={external ? 'noopener noreferrer' : undefined}
				>
					{label}
				</a>
			))
		}
	</nav>
</header>

<style>
.site-header,
	.site-header * { box-sizing: border-box; }

	.site-header {
		position: fixed;
		top: var(--space-page-reset);
		left: var(--space-page-reset);
		z-index: var(--navigation-layer);
		width: var(--navigation-width);
		padding-top: var(--space-navigation-pill-top-gap);
		pointer-events: none;
		font-family: var(--font-family-page);
		font-weight: var(--font-weight-navigation);
		line-height: var(--line-height-page);
	}

	.site-header__inner {
		position: relative;
		z-index: var(--navigation-pill-layer);
		display: flex;
		align-items: center;
		justify-content: space-between;
		width: var(--navigation-content-width);
		max-width: var(--navigation-content-max-width);
		margin: var(--space-page-reset) var(--space-centered);
		padding: var(--space-navigation-pill-block) var(--space-navigation-pill-inline);
		gap: var(--space-navigation-link-gap);
		pointer-events: auto;
		background: var(--color-navigation-pill-surface);
		border: var(--card-border-width) solid var(--color-standard-border);
		border-radius: var(--navigation-pill-radius);
		box-shadow: var(--shadow-navigation-pill);
		transition:
			background-color var(--duration-navigation-scroll) var(--easing-site-standard),
			border-color var(--duration-navigation-scroll) var(--easing-site-standard),
			box-shadow var(--duration-navigation-scroll) var(--easing-site-standard);
	}

	.site-header[data-scrolled='true'] .site-header__inner {
		background: var(--color-navigation-pill-scrolled);
		border-color: var(--color-navigation-scrolled-border);
	}

	.site-header__brand {
		display: flex;
		align-items: center;
		gap: var(--space-navigation-brand-gap);
		font-size: var(--font-size-navigation-brand);
		font-weight: var(--font-weight-section-heading);
		line-height: var(--line-height-icon);
	}

	.site-header__mark {
		width: var(--space-centered);
		height: var(--navigation-logo-height);
		transition:
			transform var(--duration-navigation-logo-pop) var(--easing-site-out),
			opacity var(--duration-navigation-logo-pop) var(--easing-site-out);
	}

	.site-header__mark:hover { transform: scale(var(--scale-social-hover)); }

	.site-header__brand-link {
		color: var(--color-text-on-dark);
		font-size: var(--font-size-navigation-brand);
		font-weight: var(--font-weight-section-heading);
		text-decoration: none;
	}

	.site-header__brand-first { color: var(--color-text-on-dark); }
	.site-header__brand-second { color: var(--color-brand-primary); }

	.site-header__brand-link span {
		transition: color var(--duration-button-standard) var(--easing-site-standard);
	}

	.site-header__brand-link:hover .site-header__brand-first { color: var(--color-brand-primary); }
	.site-header__brand-link:hover .site-header__brand-second { color: var(--color-text-on-dark); }

	.site-header__links {
		display: var(--navigation-links-display);
		align-items: center;
		gap: var(--space-navigation-link-gap);
	}

	.site-header__links a {
		display: flex;
		color: var(--color-supporting-text);
		font-size: var(--font-size-action-label);
		font-weight: var(--font-weight-navigation);
		text-decoration: none;
		transition: color var(--duration-button-standard) var(--easing-site-standard);
	}

	.site-header__links a:hover,
	.site-header__links a[aria-current='location'] { color: var(--color-brand-primary); }

	.site-header__toggle {
		position: relative;
		display: var(--navigation-toggle-display);
		align-items: center;
		justify-content: center;
		width: var(--navigation-mobile-control-size);
		height: var(--navigation-mobile-control-size);
		padding: var(--space-page-reset);
		overflow: hidden;
		color: var(--color-clear-surface);
		cursor: pointer;
		background: var(--color-interactive-surface);
		border: var(--card-border-width) solid var(--color-standard-border);
		border-radius: var(--radius-action-standard);
		transition:
			background-color var(--duration-button-standard) var(--easing-site-standard),
			border-color var(--duration-button-standard) var(--easing-site-standard);
	}

	.site-header__toggle::before,
	.site-header__toggle::after {
		position: absolute;
		left: var(--layout-center-position);
		width: var(--navigation-toggle-line-width);
		height: var(--navigation-toggle-line-height);
		content: '';
		background: var(--color-brand-primary);
		border-radius: var(--radius-action-standard);
		transform: translate(var(--layout-center-translation), var(--layout-center-translation));
		transition:
			top var(--duration-navigation-overlay-slide) var(--easing-site-out),
			transform var(--duration-navigation-overlay-slide) var(--easing-site-out),
			background-color var(--duration-navigation-overlay-slide) var(--easing-site-standard);
	}

	.site-header__toggle::before {
		top: calc(var(--layout-center-position) - var(--navigation-toggle-line-offset));
	}

	.site-header__toggle::after {
		top: calc(var(--layout-center-position) + var(--navigation-toggle-line-offset));
	}

	.site-header__toggle[aria-expanded='true'] {
		background: var(--color-brand-primary);
		border-color: var(--color-brand-primary);
	}

	.site-header__toggle[aria-expanded='true']::before,
	.site-header__toggle[aria-expanded='true']::after {
		top: var(--layout-center-position);
		background: var(--color-page-background);
	}

	.site-header__toggle[aria-expanded='true']::before {
		transform: translate(var(--layout-center-translation), var(--layout-center-translation)) rotate(var(--navigation-toggle-open-rotation));
	}

	.site-header__toggle[aria-expanded='true']::after {
		transform: translate(var(--layout-center-translation), var(--layout-center-translation)) rotate(var(--navigation-toggle-close-rotation));
	}

	.site-header__toggle:hover {
		background: var(--color-raised-card-surface);
		border-color: var(--color-hover-border);
	}

	.site-header__toggle[aria-expanded='true']:hover {
		background: var(--color-brand-action-secondary);
		border-color: var(--color-brand-action-secondary);
	}

	.site-header__overlay {
		position: fixed;
		top: var(--navigation-overlay-top);
		left: var(--layout-center-position);
		z-index: var(--navigation-overlay-layer);
		display: var(--navigation-overlay-display);
		flex-direction: column;
		width: var(--navigation-overlay-width);
		height: var(--navigation-overlay-height);
		padding: var(--space-navigation-overlay-inline);
		gap: var(--space-navigation-overlay-gap);
		visibility: hidden;
		opacity: var(--opacity-entry-start);
		pointer-events: none;
		background: var(--color-mobile-navigation-backdrop);
		border: var(--card-border-width) solid var(--color-standard-border);
		border-radius: var(--radius-card-standard);
		box-shadow: var(--shadow-navigation-pill);
		transform: translate(var(--layout-center-translation), var(--navigation-overlay-closed-position));
		transition:
			opacity var(--duration-navigation-overlay-slide) var(--easing-site-out),
			transform var(--duration-navigation-overlay-slide) var(--easing-site-out),
			visibility var(--duration-navigation-overlay-slide) var(--easing-site-out);
	}

	.site-header__overlay[data-open='true'] {
		visibility: visible;
		opacity: var(--opacity-entry-end);
		pointer-events: auto;
		transform: translate(var(--layout-center-translation), var(--navigation-overlay-open-position));
	}

	.site-header__overlay a {
		display: flex;
		align-items: center;
		min-height: var(--navigation-overlay-link-height);
		padding-inline: var(--space-navigation-overlay-link-inline);
		color: var(--color-supporting-text);
		font-size: var(--font-size-feature-copy);
		font-weight: var(--font-weight-navigation);
		text-decoration: none;
		border-radius: var(--radius-action-standard);
		transition:
			color var(--duration-button-standard) var(--easing-site-standard),
			background-color var(--duration-button-standard) var(--easing-site-standard);
	}

	.site-header__overlay a:hover,
	.site-header__overlay a:focus-visible {
		color: var(--color-text-on-dark);
		background: var(--color-interactive-surface);
	}
</style>

<section
	class="community section-jump-target"
	id="community"
	data-reveal="fade-up"
>
	<div class="content-container community__content">
		<h2 class="section-heading">Our Community</h2>
		<p class="section-copy">
			We connect passionate people working on open-source projects, hosting
			events, and driving innovation challenges.
		</p>
	</div>
</section>

<style>
	.community {
		width: var(--section-full-width);
		padding: var(--space-parent-section-block) var(--space-page-reset);
		background: var(--color-page-background);
	}

	.community__content {
		max-width: var(--parent-copy-max-width);
		text-align: center;
	}

	.community .section-heading {
		color: var(--color-brand-primary);
	}

	.community .section-copy {
		margin-inline: var(--space-centered);
	}
</style>

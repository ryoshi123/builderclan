<section
	class="community section-jump-target"
	id="community"
	data-reveal="fade-up"
>
	<div class="content-container community__content">
		<h2 class="section-heading">Our Community</h2>
		<p class="section-copy">
			We connect passionate people working on open-source projects, hosting
			events, and driving innovation challenges.
		</p>
	</div>
</section>

<style>
	.community {
		position: relative;
		width: var(--section-full-width);
		padding: var(--space-community-section-top) var(--space-section-gutter)
			var(--space-community-section-bottom);
		overflow: hidden;
	}

	.community::before {
		position: absolute;
		inset: var(--section-ambient-inset);
		content: '';
		pointer-events: none;
		background: var(--background-section-ambient-reverse);
		filter: blur(var(--section-ambient-blur));
	}

	.community__content {
		position: relative;
		text-align: center;
	}
</style>
